How to create executable of version.
Copy most recent file into "tomake"; name "decryptitgame.py"
Replace the "with open..." with:
	import quotesl
	quotes = quotesl.getall()
Copy txt from txt file into the string in 'quotesl.py'

RUN decryptitgame.py AND MAKE SURE IT WORKS
DOUBLE CHECK

If sure it works,
Double click 'make.command'